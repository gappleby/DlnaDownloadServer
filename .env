# IP address of your Fetch TV box
FETCH_HOST=10.0.0.14

# DLNA port — Fetch TV uses 49152, only change if yours differs
FETCH_PORT=49152

# Where downloaded files are written on the host
OUTPUT_DIR=./recordings
